patients:
  entry_type_enabled: True
  max_granularity: record
  endpoint_name: patients
  open_api_definition: https://github.com/EGA-archive/EUCAIM-Schema/blob/main/PatientsMetadata.json
  allow_queries_without_filters: True
  allow_id_query: True # endpoint_name/{id}
  response_type: non_collection
  connection:
    name: mongo
    database: beacon
    table: patients
    functions:
      function_name_assigned: get_endpoint
      id_query_function_name_assigned: get_endpoint_with_id
  info:
    name: Patient for the set of images
    ontology_id: ICO:0000422
    ontology_name: Patient
    description: 
  schema:
    specification: EUCAIM v0
    default_schema_id: EUCAIM-patient_v0.0.1
    default_schema_name: Default schema for a patient
    reference_to_default_schema_definition: https://github.com/EGA-archive/EUCAIM-Schema/blob/main/PatientsMetadata.json
    default_schema_version: v0.0.1
    supported_schemas:
      - EUCAIM-patient_v0.0.1
  lookups:
    collections:
      endpoint_name: patients/{id}/collections
      response_type: non_collection
      endpoint_enabled: True
      connection:
        name: mongo
        database: beacon
        table: collections
        functions:
          function_name_assigned: get_endpoint_cross_query